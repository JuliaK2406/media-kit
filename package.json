{
  "name": "media-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Speaker media kit for Julia Krylova: one YAML source builds the page, the media kit PDF and the one-sheet",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node src/build.mjs",
    "check": "node src/check.mjs",
    "pdf": "node scripts/pdf.mjs"
  },
  "dependencies": {
    "js-yaml": "^5.4.1",
    "playwright": "^1.62.1",
    "sharp": "^0.35.4"
  }
}
